<nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
    <div class="container-fluid">
        <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">PicnicFinder</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
            <ul class="navbar-nav flex-grow-1 align-items-center">
                <li class="nav-item @((ViewData["ActiveMenu"]?.ToString() == "Dashboard") ? "active" : "")">
                    <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">
                        Des espaces
                    </a>
                </li>
                <li class="nav-item ml-auto">
                    <div class="input-group" style="height: 30px;"> <!-- Ajustez la hauteur ici -->
                        <div class="input-group-prepend">
                            <span class="input-group-text" style="height: 100%; display: flex; align-items: center;">
                                <i class="fa fa-search"></i> <!-- Icône de recherche -->
                            </span>
                        </div>
                        <input type="text" class="form-control" placeholder="Rechercher quelque chose..."
                            style="height: 100%;">
                    </div>
                </li>

            </ul>


            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <form action="@Url.Action("Logout", "Auth")" method="post">
                        <button type="submit" class="btn btn-link nav-link text-dark">Me Connecter</button>
                    </form>
                </li>
                <li class="nav-item">
                    <form action="@Url.Action("Logout", "Auth")" method="post">
                        <button type="submit" class="btn btn-link nav-link text-dark">Créer un compte</button>
                    </form>
                </li>
            </ul>
        </div>
    </div>
</nav>